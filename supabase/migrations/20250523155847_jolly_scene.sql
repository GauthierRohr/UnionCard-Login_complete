/*
  # Empty migration - has_card column already exists

  1. Changes
    - Removed duplicate has_card column definition as it was already added in previous migration

  2. Security
    - No changes to existing RLS policies
*/

-- Column has_card already exists from previous migration